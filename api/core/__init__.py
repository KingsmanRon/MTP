"""Inntris Core — vendor-neutral domain layer.

This package holds the inner layer of the system: types and ports that
reason about consequential agent actions without depending on any
particular rail, connector, or external provider. Nothing here may name
a vendor, a wire format, or a storage engine.
"""
