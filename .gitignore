# =============================================================================
# INNTRIS CORE - GIT IGNORE
# =============================================================================

# -----------------------------------------------------------------------------
# Environment & Secrets (CRITICAL - Never commit these!)
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
*.pem
*.key
secrets/
.secrets

# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
# Note: we deliberately do NOT ignore top-level ``lib/`` here; Foundry keeps
# its submodules (forge-std, openzeppelin-contracts) under lib/ per Phase 2A.
# If you create a Python venv at the repo root, use ``.venv/`` (already
# ignored above) instead of the legacy ``lib/`` layout.

# Virtual environments
venv/
ENV/
env/
.venv/

# Type checking
.mypy_cache/
.dmypy.json
dmypy.json

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
*.py,cover
.hypothesis/

# -----------------------------------------------------------------------------
# IDE & Editors
# -----------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.project
.pydevproject
.settings/
*.sublime-workspace
*.sublime-project

# -----------------------------------------------------------------------------
# OS Files
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# -----------------------------------------------------------------------------
# Docker
# -----------------------------------------------------------------------------
.docker/

# -----------------------------------------------------------------------------
# Blockchain / Solidity
# -----------------------------------------------------------------------------
contracts/artifacts/
contracts/cache/
node_modules/
.openzeppelin/

# Foundry build artifacts (Phase 2A). ``out/`` is already ignored for Next.js
# below; ``cache/`` is Foundry's incremental cache. ``broadcast/`` holds the
# full history of forge-script deployments — never commit RPC/tx secrets.
cache/
broadcast/

# -----------------------------------------------------------------------------
# Logs & Databases
# -----------------------------------------------------------------------------
*.log
*.sqlite
*.db
logs/

# -----------------------------------------------------------------------------
# Build Artifacts
# -----------------------------------------------------------------------------
*.min.js
*.min.css
public/build/

# -----------------------------------------------------------------------------
# Next.js
# -----------------------------------------------------------------------------
.next/
out/
*.tsbuildinfo

# -----------------------------------------------------------------------------
# graphify (local knowledge-graph tooling — NEVER commit; local-only artifacts)
# -----------------------------------------------------------------------------
graphify-out/

# Phase-6 proof output. Both are generated, neither is committed.
#
# The evidence pack is regenerated by `python -m scripts.mastercard_vi_proof`
# on every run and its contents change every time — ES256 carries a random
# nonce, so digests and signatures differ. Committing it churned the diff and,
# more to the point, wrote hundreds of high-entropy public values next to
# key-shaped JSON field names, which the repository's gitleaks gate correctly
# flags. See docs/proofs/mastercard-vi/HANDOFF.md for which fields and why
# they are public; naming them here trips the same rule. CI runs the proof
# and uploads the pack as a workflow artifact instead.
docs/proofs/mastercard-vi/evidence/
docs/proofs/mastercard-vi/execution-journal.sqlite*
