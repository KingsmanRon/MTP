{
  "name": "inntris-verify-action",
  "version": "1.0.0",
  "private": true,
  "description": "Inntris CI Guard GitHub Action — detect changed files, classify by risk, and verify with Inntris.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --minify --no-source-map-register",
    "test": "node --test *.test.js"
  },
  "dependencies": {
    "js-yaml": "^4.3.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.0"
  }
}
